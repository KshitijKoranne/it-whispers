{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "corepack enable && corepack yarn install --immutable --mode=skip-build",
  "buildCommand": "corepack yarn workspace web build",
  "outputDirectory": "apps/web/.next"
}
